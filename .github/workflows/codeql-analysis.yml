name: CodeQL Analysis

on:
  workflow_dispatch:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up CodeQL
        uses: github/codeql-action/setup@a6c67f2  # Specific commit SHA
        with:
          languages: 'java'

      - name: Auto-detect CodeQL configuration
        uses: github/codeql-action/autodetect@a6c67f2  # Specific commit SHA

      - name: Run CodeQL Analysis
        uses: github/codeql-action/analyze@a6c67f2  # Specific commit SHA
        with:
          category: 'security'
